%------------------------------------------------------------------------
% Recovery process : GOAL State
%------------------------------------------------------------------------
finally(resource_AreSameTree, boolean).
goal(I) :- exists(resource_AreSameTree,boolean,I),  step(I).
%------------------------------------------------------------------------
