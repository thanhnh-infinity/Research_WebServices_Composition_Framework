%------------------------------------------------------------------------
% GOAL State
%------------------------------------------------------------------------
finally(resource_reconcileTree, newickTree).
goal(I) :- exists(resource_reconcileTree,newickTree,I),  step(I).
%------------------------------------------------------------------------
